name: main
on:
  pull_request:
  push:
    branches:
      - "main"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Cancel in-progress runs for the workflow for pull requests.
  # For pushes to main, in-progress runs should NOT be cancelled since pushing container images should not be cancelled.
  cancel-in-progress: ${{ github.event_name != 'push' && github.ref_name != 'main' }}

jobs:
  prepare_matrix:
    runs-on: ubuntu-slim
    outputs:
      jobs: ${{ steps.prepare-matrix.outputs.jobs }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      # Build targets are defined in .github/build-targets.yaml and filtered to
      # the images whose files changed in the PR by the generate_matrix script.
      - id: prepare-matrix
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "pull_request" ]; then
            export PR=$(jq -r '.pull_request.number' "${GITHUB_EVENT_PATH}")
          else
            export PR=$(gh pr list --state merged --json number,mergeCommit | jq ".[] | select(.mergeCommit != null and .mergeCommit.oid == \"${GITHUB_SHA}\") | .number")
          fi
          ./generate_matrix
          echo "jobs=$(cat .github/outputs/__matrix.json)" >> "$GITHUB_OUTPUT"

  build_container:
    needs: prepare_matrix
    if: ${{ needs.prepare_matrix.outputs.jobs != '[]' }}
    strategy:
      matrix:
        job: ${{ fromJson(needs.prepare_matrix.outputs.jobs) }}
    runs-on: ${{ matrix.job.runner || 'ubuntu-24.04' }}
    permissions:
      contents: read
      packages: write
    env:
      go-version: "1.26.3"
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Setup build environment
        uses: ./.github/actions/setup
        with:
          # A matrix entry may override the Go version via the "go-version" field.
          go-version: ${{ matrix.job.go-version || env.go-version }}
      - name: Disk cleanup
        if: ${{ matrix.job.disk-cleanup == 'true' }}
        run: |
          # These code are copied from the following code:
          # https://github.com/shiguredo-webrtc-build/webrtc-build/blob/5a821e430b496bbff74cf45bab058ab4ac340c2c/.github/workflows/build.yml#L138-L147
          #    LICENSE: http://www.apache.org/licenses/LICENSE-2.0
          #    Copyright 2019-2025, Shiguredo Inc.
          df -h
          sudo rm -rf /usr/share/swift
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          df -h
      - name: Build container image
        uses: ./.github/actions/build_push
        with:
          push: ${{ github.event_name == 'push' && github.ref_name == 'main' }}
          dir: ${{ matrix.job.dir }}
          tags: ${{ join(matrix.job.tags, ',') }}
          platforms: ${{ matrix.job.platforms }}
          load: ${{ matrix.job.load }}
          make-targets: ${{ matrix.job.make-targets }}
          make-post-targets: ${{ matrix.job.make-post-targets }}
          target: ${{ matrix.job.target }}

  prepare_scan_matrix:
    needs:
    - prepare_matrix
    runs-on: ubuntu-slim
    outputs:
      jobs: ${{ steps.prepare-scan-matrix.outputs.jobs }}
    permissions: {}
    steps:
      - id: prepare-scan-matrix
        env:
          JOBS: ${{ needs.prepare_matrix.outputs.jobs }}
        run: |
          # Filter the build matrix for images that have scanning enabled.
          SCAN_JOBS=$(echo "$JOBS" | jq '[.[] | select(.scan.enabled) | .scan]')
          echo "$SCAN_JOBS" | jq .
          echo "jobs=$SCAN_JOBS" >> "$GITHUB_OUTPUT"

  scan_image:
    uses: ./.github/workflows/scan.yaml
    if: ${{ github.event_name == 'push' && github.ref_name == 'main' && needs.prepare_scan_matrix.outputs.jobs != '[]' }}
    permissions:
      contents: read
      packages: read
    needs:
    - prepare_scan_matrix
    - build_container
    strategy:
      matrix:
        scan: ${{ fromJson(needs.prepare_scan_matrix.outputs.jobs) }}
    with:
      image: ${{ matrix.scan.image }}
      tag: ${{ matrix.scan.tag }}
      image-identifier: ${{ matrix.scan.identifier }}
      image-is-public: ${{ matrix.scan.is_public }}
    secrets:
      yamory-token: ${{ secrets[format('YAMORY_TOKEN_TEAM_{0}', matrix.scan.team)] }}

  build_envoy:
    runs-on: neco-containers-ubuntu-22.04
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Build container image
        uses: ./.github/actions/build_envoy
        with:
          push: ${{ github.event_name == 'push' && github.ref_name == 'main' }}
          clang_archive_path: https://github.com/llvm/llvm-project/releases/download/llvmorg-18.1.8/clang+llvm-18.1.8-x86_64-linux-gnu-ubuntu-18.04.tar.xz
          clang_archive_sha: 54ec30358afcc9fb8aa74307db3046f5187f9fb89fb37064cdde906e062ebf36
          envoy_commit_sha: adb998f2875986005be294d10231a9c9a497e30d

  scan_envoy_image:
    uses: ./.github/workflows/scan.yaml
    needs: build_envoy
    if: ${{ github.event_name == 'push' && github.ref_name == 'main' && needs.build_envoy.outputs.changed == 'true' }}
    permissions:
      contents: read
      packages: read
    with:
      image: ${{ needs.build_envoy.outputs.image }}
      tag: ${{ needs.build_envoy.outputs.tag }}
      image-identifier: ${{ needs.build_envoy.outputs.image-identifier }}
      image-is-public: true
    secrets:
      yamory-token: ${{ secrets.YAMORY_TOKEN_TEAM_NECO }}

  build_ceph:
    runs-on: neco-containers-ubuntu-22.04
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
    - name: Build ceph image
      uses: ./.github/actions/build_ceph
      with:
        push: ${{ github.event_name == 'push' && github.ref_name == 'main' }}
