apiVersion: v1
kind: Service
metadata:
  namespace: neco-server-exporter
  name: neco-server-exporter
spec:
  externalTrafficPolicy: Local
  internalTrafficPolicy: Local
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/name: neco-server-exporter
  type: NodePort
